# Full Check: PubMed Search MCP

Use [the PubMed full-check workflow](pubmed-full-check.md), the canonical
validation workflow for this repository. It calls the shared local gate in
`scripts/check_repo.py`; do not run commands for sibling projects or a
`vscode-extension/` directory that does not exist here.
