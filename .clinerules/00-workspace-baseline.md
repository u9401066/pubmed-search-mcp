# Workspace baseline

- Read `AGENTS.md` as the shared baseline for this repository.
- Treat this `.clinerules/` directory as the Cline-specific overlay only.
- Do not duplicate broad repository guidance that already exists in `AGENTS.md`.
- Keep rules focused and path-scoped where possible so Cline context stays lean.
- Use workflows in `.clinerules/workflows/` for repetitive harness tasks instead of adding long always-on rules.