# Project rule entrypoint

Use `AGENTS.md` for the shared baseline and `50-pubmed-project.md` for the
Cline-specific project overlay. This repository is PubMed Search MCP; it does
not contain the Asset-Aware server or a `vscode-extension/` package.
