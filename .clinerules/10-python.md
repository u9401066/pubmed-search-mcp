# Python rule entrypoint

Use `AGENTS.md`, `10-python-ddd.md`, and `60-pubmed-python.md` for the
repository's Python boundaries and checks. Document parsing and VSIX rules
from sibling projects do not apply to this source tree.
